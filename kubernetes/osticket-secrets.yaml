apiVersion: v1
kind: Secret
metadata:
  name: osticket-secrets
  namespace: osticket
type: Opaque
data:
  # These are placeholders. In a real deployment, use proper secrets management
  # The values below are base64 encoded placeholders, replace them with your actual values
  MYSQL_USER: b3N0aWNrZXQ=  # osticket
  MYSQL_PASSWORD: cGFzc3dvcmQ=  # password
